INCLUDE_DIRECTORIES("${CMAKE_CURRENT_SOURCE_DIR}")

ADD_SUBDIRECTORY(command)
LIST(APPEND LABEL_CORE_DEP_LIBS label_core_command)

AUX_SOURCE_DIRECTORY(. LABEL_CORE_SRC)
ADD_LIBRARY(label_core ${LABEL_CORE_SRC})
TARGET_LINK_LIBRARIES(label_core ${LABEL_CORE_DEP_LIBS})
